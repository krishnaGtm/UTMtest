/**
 * Created by psindurakar on 11/29/2017.
 */

/*
export const addMarker = action => {
    return {
        type: 'MARKER_ADD',
        determinationAlias: action.determinationAlias,
        determinationID: action.determinationID,
        determinationName: action.determinationName,
        selected: false
    }
}

export const toggleMarker = action => {

} */
